function initMap() {
    // Eğer API anahtarı yoksa bu fonksiyon çalışmaz.
    // Test için basit bir alert veya konsol logu:
    console.log("Harita başlatılıyor...");
    
    // Gerçek harita kodu:
    /*
    const ankara = { lat: 39.9334, lng: 32.8597 };
    const map = new google.maps.Map(document.getElementById("map"), {
      zoom: 6,
      center: ankara,
    });
    const marker = new google.maps.Marker({
      position: ankara,
      map: map,
      title: "Mobbing ile Mücadele Derneği",
    });
    */
}
// Bu dosyayı HTML'de Google scriptinden SONRA çağırmalısınız.
